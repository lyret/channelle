# ============================================================================
# Channelle - Environment Configuration Example
# ============================================================================
# Copy this file to .env and modify the values as needed for your deployment.
# All environment variables are optional unless marked as [REQUIRED].
# CLI flags will override environment variables when provided.

# ============================================================================
# STAGE CONFIGURATION
# ============================================================================

# Stage name for this deployment
# CLI flag: --name
# Optional: Yes, defaults to empty string
# STAGE_NAME=My Performance Stage

# Invite link key for password-protected stages
# CLI flag: --inviteKey
# Optional: Yes, defaults to empty string (public stage)
# STAGE_INVITE_LINK_KEY=secret_invite_key_123

# Unique identifier for this stage deployment
# CLI flag: --id
# Optional: Yes, defaults to slugified stage name
# STAGE_ID=my-stage-id

# Password for theater mode authentication
# CLI flag: --theaterPassword
# Optional: Yes, defaults to "admin"
# THEATER_PASSWORD=secure_theater_password

# Show ID to initialize stage with (stage mode only)
# CLI flag: --showId
# Optional: Yes, no default
# SHOW_ID=1

# ============================================================================
# RUNTIME CONFIGURATION
# ============================================================================

# Enable production mode (affects security, logging, and performance)
# CLI flag: --production
# Optional: Yes, defaults to false (or true if NODE_ENV=production)
# PRODUCTION=false

# Server port number
# CLI flag: --port
# Optional: Yes, defaults to 3000
# PORT=3000

# Enable verbose logging output
# CLI flag: --verbose
# Optional: Yes, defaults to false
# VERBOSE=false

# Enable debug mode (enables Node.js debugger support)
# CLI flag: --debug
# Optional: Yes, defaults to false
# DEBUG=false

# Enable local network access (loopback interface)
# CLI flag: --local
# Optional: Yes, defaults to true
# LOCAL=true

# Enable LAN network access (local area network)
# CLI flag: --lan
# Optional: Yes, defaults to true
# LAN=true

# Enable WAN network access (wide area network/internet)
# CLI flag: --wan
# Optional: Yes, defaults to production value
# WAN=false

# Enable build mode (generate fresh code bundles)
# CLI flag: --build
# Optional: Yes, defaults to false
# BUILD=false

# Enable watch mode (rebuild on file changes)
# CLI flag: --watch
# Optional: Yes, defaults to false
# WATCH=false

# Enable start mode (launch the server)
# CLI flag: --start
# Optional: Yes, defaults to false
# START=false

# Enable theater mode (management interface)
# CLI flag: --theater
# Optional: Yes, defaults to false (stage mode)
# THEATER=false

# ============================================================================
# LAUNCHER ADAPTER CONFIGURATION (Theater Mode Only)
# ============================================================================

# ACTIVE ADAPTER SELECTION
# ------------------------

# Which adapter to use for launching stages (none, local, digitalocean)
# CLI flag: --theater-adapter
# Optional: Yes, defaults to "none" (no launching capability)
# THEATER_ADAPTER=none

# LOCAL ADAPTER SETTINGS
# ----------------------
# These settings are only used when THEATER_ADAPTER=local

# Maximum number of local stage instances that can be launched simultaneously
# CLI flag: --theater-adapter-local-max-stages
# Optional: Yes, defaults to 1
# THEATER_ADAPTER_LOCAL_MAX_STAGES=1

# DIGITALOCEAN ADAPTER SETTINGS
# -----------------------------
# These settings are only used when THEATER_ADAPTER=digitalocean

# DigitalOcean API key for creating and managing droplets
# CLI flag: --theater-adapter-digitalocean-api-key
# Optional: Yes, no default (required for DigitalOcean adapter to function)
# THEATER_ADAPTER_DIGITALOCEAN_API_KEY=dop_v1_your_api_key_here

# Maximum number of DigitalOcean VPN servers that can be launched
# CLI flag: --theater-adapter-digitalocean-max-vpns
# Optional: Yes, defaults to 5
# THEATER_ADAPTER_DIGITALOCEAN_MAX_VPNS=5

# ============================================================================
# ADVANCED CONFIGURATION
# ============================================================================
# The following options are typically only needed for advanced deployments
# or when the defaults don't work for your specific environment.

# Node.js environment (affects various framework behaviors)
# Optional: Yes, defaults based on PRODUCTION flag
# NODE_ENV=development

# ============================================================================
# USAGE EXAMPLES
# ============================================================================
#
# Development stage server:
# PORT=3000
# DEBUG=true
# VERBOSE=true
#
# Production stage server:
# PRODUCTION=true
# PORT=80
# STAGE_NAME=Main Performance
# THEATER_PASSWORD=secure_production_password
# WAN=true
#
# Theater mode with local adapter for show management:
# THEATER=true
# THEATER_PASSWORD=theater_admin_password
# THEATER_ADAPTER=local
# THEATER_ADAPTER_LOCAL_MAX_STAGES=2
#
# Theater mode with DigitalOcean adapter:
# THEATER=true
# THEATER_PASSWORD=theater_admin_password
# THEATER_ADAPTER=digitalocean
# THEATER_ADAPTER_DIGITALOCEAN_API_KEY=dop_v1_your_real_api_key
# THEATER_ADAPTER_DIGITALOCEAN_MAX_VPNS=10
